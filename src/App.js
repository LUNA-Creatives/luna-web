import { ThemeConfig } from './theme/ThemeConfig';
import { Routing } from './routing';

const App = () => {
  return (
    <ThemeConfig>
      <Routing />
    </ThemeConfig>
  );
};

export default App;
